plugins {
    id 'com.android.application'
    id 'org.jetbrains.kotlin.android'
}

android {
    namespace "com.example.ai_amoz_flutter"

    compileSdk 35

    defaultConfig {
        applicationId "com.example.ai_amoz_flutter"

        minSdk 23
        targetSdk 35

        versionCode 1
        versionName "1.0"
    }

    buildTypes {
        release {
            minifyEnabled false
        }
    }
}

flutter {
    source "../.."
}

dependencies {
}
